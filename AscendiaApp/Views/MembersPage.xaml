<Page x:Class="AscendiaApp.Views.MembersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:adapters="using:LCTWorks.WinUI.Xaml.Adapters"
      xmlns:core="using:Microsoft.Xaml.Interactivity"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:helpers="using:AscendiaApp.Helpers"
      xmlns:i="using:Microsoft.Xaml.Interactivity"
      xmlns:lctWorks="using:LCTWorks.WinUI.Controls"
      xmlns:local="using:AscendiaApp.Views"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:observable="using:AscendiaApp.Observable"
      xmlns:toolkit="using:CommunityToolkit.WinUI.Controls"
      mc:Ignorable="d">

    <Page.Resources>
        <SolidColorBrush x:Key="ListViewItemBackground">#FF2D2D2D</SolidColorBrush>
        <Style BasedOn="{StaticResource DefaultGridViewItemStyle}"
               TargetType="GridViewItem">
            <Setter Property="HorizontalContentAlignment"
                    Value="Stretch" />
        </Style>
        <XamlUICommand x:Key="EditMemberXamlUICommand"
                       Command="{x:Bind ViewModel.EditMemberCommand}" />
        <XamlUICommand x:Key="RemoveMemberXamlUICommand"
                       Command="{x:Bind ViewModel.RemoveMemberCommand}" />
    </Page.Resources>
    <i:Interaction.Behaviors>
        <core:EventTriggerBehavior EventName="Loaded">
            <core:InvokeCommandAction Command="{x:Bind ViewModel.InitializeCommand}" />
        </core:EventTriggerBehavior>
    </i:Interaction.Behaviors>

    <Grid Margin="0,8"
          RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid Padding="8"
              Background="{StaticResource AppColor3}"
              ColumnSpacing="8"
              CornerRadius="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"
                                  MaxWidth="400" />
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <AutoSuggestBox x:Uid="Members-SearchBox"
                            QueryIcon="Find"
                            TabIndex="0"
                            Text="{x:Bind ViewModel.SearchTerm, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <StackPanel Grid.Column="2"
                        Orientation="Horizontal"
                        Spacing="8">
                <lctWorks:ThemedButton Command="{x:Bind ViewModel.AddMemberCommand}"
                                       Content="Nuevo"
                                       Glyph="Add"
                                       GlyphFontFamily="{StaticResource MaterialSymbolsFontFamily}"
                                       IsEnabled="{x:Bind ViewModel.NotLoading, Mode=OneWay}"
                                       Style="{StaticResource AccentThemedButtonStyle}" />

                <Button VerticalAlignment="Stretch"
                        Command="{x:Bind ViewModel.RefreshCommand}"
                        IsEnabled="{x:Bind ViewModel.NotLoading, Mode=OneWay}">
                    <FontIcon FontFamily="{StaticResource MaterialSymbolsFontFamily}"
                              Glyph="Refresh" />
                </Button>
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1"
              Background="{StaticResource AppColor3}"
              CornerRadius="8">
            <ListView Background="Transparent"
                      IsItemClickEnabled="True"
                      ItemsSource="{x:Bind ViewModel.Members, Mode=OneWay}"
                      SelectedItem="{x:Bind ViewModel.SelectedMember, Mode=TwoWay}"
                      SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="observable:MemberObservable">
                        <Grid Padding="0,8"
                              Background="Transparent"
                              ColumnSpacing="8"
                              RowSpacing="2">
                            <Grid.ContextFlyout>
                                <MenuFlyout>
                                    <MenuFlyoutItem MinWidth="160"
                                                    Command="{StaticResource EditMemberXamlUICommand}"
                                                    CommandParameter="{x:Bind}"
                                                    Text="Editar">
                                        <MenuFlyoutItem.Icon>
                                            <FontIcon Glyph="&#xE70F;" />
                                        </MenuFlyoutItem.Icon>
                                    </MenuFlyoutItem>
                                    <MenuFlyoutItem CommandParameter="{x:Bind}"
                                                    IsEnabled="False"
                                                    Text="Copiar Id">
                                        <MenuFlyoutItem.Icon>
                                            <FontIcon Glyph="&#xE8C8;" />
                                        </MenuFlyoutItem.Icon>
                                    </MenuFlyoutItem>

                                    <MenuFlyoutSubItem IsEnabled="False"
                                                       Text="Abrir...">
                                        <MenuFlyoutSubItem.Icon>
                                            <FontIcon Glyph="&#xE8A7;" />
                                        </MenuFlyoutSubItem.Icon>
                                        <MenuFlyoutItem MinWidth="160"
                                                        Text="Perfil de OpenDota" />
                                    </MenuFlyoutSubItem>
                                    <MenuFlyoutSeparator />
                                    <MenuFlyoutItem Command="{StaticResource RemoveMemberXamlUICommand}"
                                                    CommandParameter="{x:Bind}"
                                                    Text="Eliminar">
                                        <MenuFlyoutItem.Icon>
                                            <FontIcon Glyph="&#xE74D;" />
                                        </MenuFlyoutItem.Icon>
                                    </MenuFlyoutItem>
                                </MenuFlyout>
                            </Grid.ContextFlyout>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width=".5*"
                                                  MinWidth="240" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="1*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Grid Grid.RowSpan="2"
                                  Width="72"
                                  Height="72"
                                  Margin="8,0">
                                <Ellipse Stroke="{StaticResource AppColor4}"
                                         StrokeThickness="2">
                                    <Ellipse.Fill>
                                        <ImageBrush ImageSource="{x:Bind AvatarUrl, Mode=OneWay}" />
                                    </Ellipse.Fill>
                                </Ellipse>
                                <Grid Width="32"
                                      Height="32"
                                      HorizontalAlignment="Right"
                                      VerticalAlignment="Bottom">
                                    <Ellipse Fill="{StaticResource AppColor1}"
                                             Stroke="{StaticResource AppColor4}"
                                             StrokeThickness="2" />
                                    <Image Width="24"
                                           Height="24"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Source="{x:Bind helpers:AssetsHelper.GetPositionImageSource(Position)}" />
                                </Grid>
                            </Grid>
                            <Grid Grid.Column="1"
                                  Margin="2"
                                  VerticalAlignment="Center">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <!--<RowDefinition Height="Auto" />-->
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.ColumnSpan="2"
                                           Margin="0,0,0,4"
                                           HorizontalTextAlignment="Left"
                                           Style="{StaticResource AppItemTitleTextBlockStyle}"
                                           Text="{x:Bind DisplayName, Mode=OneWay}" />
                                <TextBlock Grid.Row="1"
                                           Grid.ColumnSpan="2"
                                           HorizontalTextAlignment="Left"
                                           Opacity=".6"
                                           Style="{StaticResource SmoothItemCaptionTextBlockStyle}"
                                           Text="{x:Bind AccountName, Mode=OneWay}" />
                                <!--<TextBlock Grid.Row="2"
                                           Margin="0,4,4,0"
                                           FontFamily="{StaticResource MaterialSymbolsFontFamily}"
                                           Foreground="{StaticResource HighlightColor}"
                                           Style="{StaticResource SmoothItemCaptionTextBlockStyle}"
                                           Text="crown"
                                           Visibility="{x:Bind IsCaptain}" />
                                <TextBlock Grid.Row="2"
                                           Grid.Column="1"
                                           Margin="0,4,4,0"
                                           VerticalAlignment="Center"
                                           HorizontalTextAlignment="Left"
                                           Style="{StaticResource SmoothItemCaptionTextBlockStyle}"
                                           Text="{x:Bind Team, Mode=OneWay}"
                                           TextLineBounds="Tight" />-->
                            </Grid>
                            <Border Grid.Column="2"
                                    Width="120"
                                    Style="{StaticResource ItemChipBorderStyle}">
                                <StackPanel Orientation="Horizontal"
                                            Spacing="8">
                                    <Image Width="42"
                                           Source="{x:Bind helpers:AssetsHelper.GetRankImageSource(RankTier)}" />
                                    <TextBlock VerticalAlignment="Center"
                                               Text="{x:Bind Leaderboard}" />
                                </StackPanel>
                            </Border>
                            <Border Grid.Column="3"
                                    Width="120"
                                    Style="{StaticResource ItemChipBorderStyle}">
                                <StackPanel Orientation="Horizontal"
                                            Spacing="4">
                                    <TextBlock VerticalAlignment="Center"
                                               FontFamily="{StaticResource MaterialSymbolsFontFamily}"
                                               FontSize="24"
                                               Foreground="{StaticResource HighlightColor}"
                                               Text="swap_vert"
                                               TextLineBounds="Tight" />
                                    <TextBlock VerticalAlignment="Center"
                                               Text="{x:Bind GetWinRate()}"
                                               TextAlignment="Center" />
                                </StackPanel>
                            </Border>
                            <StackPanel Grid.Column="5"
                                        Margin="0,0,8,0"
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Center"
                                        Orientation="Horizontal"
                                        Spacing="4">
                                <TextBlock Foreground="{StaticResource WarningColor}"
                                           Style="{StaticResource MaterialSymbolsTextBlockStyle}"
                                           Text="schedule"
                                           TextLineBounds="Tight"
                                           Visibility="{x:Bind IsOutDated}" />
                                <TextBlock Foreground="{StaticResource SuccessColor}"
                                           Style="{StaticResource MaterialSymbolsTextBlockStyle}"
                                           Text="check_circle"
                                           TextLineBounds="Tight"
                                           Visibility="{x:Bind IsUpToDate}" />
                            </StackPanel>
                            <!--<Button Grid.Column="6"
                                    Content="..." />-->
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <Border VerticalAlignment="Stretch"
                    Background="{StaticResource AppColor3}"
                    Opacity=".4"
                    Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay}">
                <StackPanel VerticalAlignment="Center"
                            Orientation="Vertical"
                            Spacing="16">
                    <ProgressRing IsActive="True" />
                    <TextBlock MaxWidth="320"
                               HorizontalAlignment="Center"
                               Text="{x:Bind ViewModel.LoadingNotification, Mode=OneWay}"
                               TextAlignment="Center"
                               TextWrapping="WrapWholeWords" />
                    <Button x:Uid="Members-CancelButton"
                            HorizontalAlignment="Center"
                            Command="{x:Bind ViewModel.CancelCommand}"
                            IsEnabled="{x:Bind ViewModel.CancelEnabled, Mode=OneWay}"
                            Visibility="{x:Bind ViewModel.CancelVisibile, Mode=OneWay}" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>